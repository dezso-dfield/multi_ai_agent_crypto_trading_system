# works with YF:
SYMBOL=BTC-USD

# OR exchange-style (will fall back to CCXT if YF fails):
# SYMBOL=BTC/USDT
# SYMBOL=BTCUSDT
EXCHANGE_ID=binance

TIMEFRAME=1h
PAPER_START_BALANCE=10000
EXECUTION_MODE=paper

# Exchange creds (for later, when you go live)
EXCHANGE_ID=binance
API_KEY=
API_SECRET=
PASSWORD=

LLM_PROVIDER=ollama          # ollama | (together/openai later)
LLM_MODEL=llama3:8b          # any model you pulled in Ollama
OLLAMA_BASE_URL=http://localhost:11434   # or http://host.docker.internal:11434 inside Docker
LLM_TIMEOUT_SECS=30